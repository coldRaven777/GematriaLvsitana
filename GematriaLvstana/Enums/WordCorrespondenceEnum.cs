namespace GematriaLvistana.Enums
{
    public enum WordCorrespondenceEnum
 
    {
        A = 1,     // Aleph (א)
        B = 2,     // Bet (ב)
        D = 4,     // Dalet (ד)
        E = 5,     // He (ה)
        F = 80,    // Pe (פ)
        G = 3,     // gimel
        H = 5,     // He (ה)
        I = 10,     // Yod (י)
        J = 10,    // Yod (י)
        K = 100,    // Kaf (ק)
        L = 30,    // Lamed (ל)
        M = 40,    // Mem (מ)
        N = 50,    // Nun (נ)
        O = 70,    // ayin? 
        P = 80,    // Pe (פ)
        Q = 100,   // Qof (ק)
        R = 200,   // Resh (ר)
        S = 300,   // Shin (ש)
        T = 400,   // Tav (ת)
        U = 6,     // Vav (ו)
        V = 6,     // Vav (ו)
        W = 6,     // Vav (ו)
        X = 60,    // Samekh (ס) or Samekh-Tzadi
        Y = 10,    // Yod (י)
        Z = 7      // Zayin (ז)
    }
}
